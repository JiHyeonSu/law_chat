{
  "mcpServers": {
    "lawchat": {
      "command": "python",
      "args": ["mcp_server/server.py"],
      "transport": "stdio",
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      },
      "timeout": 30000
    }
  }
}
